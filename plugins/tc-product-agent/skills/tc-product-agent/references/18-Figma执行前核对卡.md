# Figma 执行前核对卡

版本：v0.4｜用途：在实际 Figma 写入前确认目标、权限、边界和证据要求；它是 D1 设计就绪的必要检查，不替代产品原型、设计任务包或产品评审。

## 1. 适用与状态

高保真 Demo、Figma 生成、设计稿补页或既有画板精修，均在执行前创建 `product/handoff/design/DESIGN-<模块>-执行前核对卡.md`。没有实际 Figma 写入时，不必创建空卡；可以生成“未绑定 Figma 目标”的设计任务包，并明确其状态为“材料已准备，未进入 D1”。

## 2. 必须核对的信息

| 项目 | 必须记录 | 不满足时的处理 |
| --- | --- | --- |
| 产品输入 | 当前 `01` 版本、SC/UC、R、UX、AC 及设计任务包版本。 | 返回产品/设计任务包补齐，不开始写入。 |
| 目标定位 | Figma 文件名/链接或稳定标识；页面；Frame/选区；目标对象的当前版本。 | 只准备任务包，不能声称设计就绪。 |
| 执行者与工具 | Figma 执行者（Agent/设计师）、可用工具、账户/会话和真实可执行能力。 | 改为交给设计方的任务包，或等待条件具备。 |
| 授权 | 谁授权、授权日期、允许的动作和范围。 | 不读取/写入/覆盖外部设计。 |
| 写入范围 | 可创建、修改、移动、删除的页面/Frame/图层范围。 | 不执行可能影响既有内容的动作。 |
| 受保护对象 | 不得覆盖、移动、删除或改写的页面、组件、变量、资产和既有已确认 Frame。 | 将对象列入任务包禁止项，无法识别则受阻。 |
| 视觉基线 | 设计系统/组件库、品牌资产、内容密度、响应式与无障碍约束。 | 标为探索范围，不把猜测写成视觉基线。 |
| 证据与回传 | 应返回的页面/节点、截图/导出、状态覆盖、差异和责任人。 | 不开始 D2 回审或声称设计完成。 |

## 3. 可直接复用的核对卡

```markdown
# Figma 执行前核对卡：<模块>

- cardId / version：<唯一标识>。
- 当前状态：{待补齐 / D1 通过，可执行 / 已执行待回传 / 已替代}。
- 产品与任务包输入：{01、SC/UC、R、UX、AC、DesignTaskPackage 版本}。
- 执行者与工具：{姓名/Agent、工具、会话/能力状态}。
- 授权依据：{谁、何时、允许做什么}。

## Figma 目标与写入边界
| 项目 | 已确认内容 | 证据/链接 | 状态 |
| --- | --- | --- | --- |
| 文件与页面 |  |  |  |
| Frame/选区 |  |  |  |
| 允许动作 |  |  |  |
| 受保护对象 |  |  |  |
| 组件/变量/资产基线 |  |  |  |

## 回传要求
| 页面/状态 | 需返回的节点或导出 | 关联 SC/R/UX/AC | 验收责任人 |
| --- | --- | --- | --- |

## 结论
- D1：{通过 / 未通过}。
- 未通过项、影响与恢复：{Q-ID、责任方、下一动作}。
```

## 4. 通过与停止规则

D1 仅在上表所有必填项可定位、产品基线没有影响本范围的阻断、设计任务包可观察验收完整时通过。目标、授权、写入范围或受保护对象任一未知时：可以继续产出或修改任务包，但不得读取/写入 Figma，不得称“等待 Figma 执行”，也不得把设计任务包的生成视为设计通过。

执行完成只进入“已执行待回传”。只有产品负责人根据真实 Figma 节点/截图/导出和覆盖矩阵完成回审，才能进入 D2。
